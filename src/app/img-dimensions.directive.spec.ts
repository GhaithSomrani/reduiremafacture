import { ImgDimensionsDirective } from './img-dimensions.directive';

describe('ImgDimensionsDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgDimensionsDirective();
    expect(directive).toBeTruthy();
  });
});
